/* Solution-style tsconfig: delegates to sub-project configs for correct type resolution in the IDE. */
/* Shared compiler options are in tsconfig.base.json. */
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.spec.json" }
  ]
}
